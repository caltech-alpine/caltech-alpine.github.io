# Usage logs. Public data in aggregate, but there is no reason to serve the
# raw files, and the salt in here must never be readable.
<IfModule mod_authz_core.c>
  Require all denied
</IfModule>
<IfModule !mod_authz_core.c>
  Order allow,deny
  Deny from all
</IfModule>
