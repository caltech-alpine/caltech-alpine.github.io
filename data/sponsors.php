<?php
/**
 * ============================================================================
 *  SPONSORS  —  add a supporter here and they appear on the homepage and on
 *  the Support Us page. Remove them and they disappear everywhere.
 * ============================================================================
 *
 *  Fields
 *    name    required
 *    url     optional — links the logo
 *    logo    optional file in assets/images/sponsors/
 *            SVG is best; otherwise a PNG with a transparent background.
 *            With no logo file we print the name as a wordmark, which looks
 *            deliberate, so a sponsor can be listed the day they say yes.
 *    tier    optional label, e.g. 'Supporting', 'Partner'
 *    note    optional one line about what they support
 *
 *  While this list is empty the homepage shows a short "become a sponsor"
 *  invitation instead of an empty shelf of logos.
 * ============================================================================
 */

return array(

    // Example — delete this comment block and uncomment to start:
    //
    // array(
    //     'name' => 'Example Outdoor Co.',
    //     'url'  => 'https://example.com',
    //     'logo' => 'sponsors/example.svg',
    //     'tier' => 'Supporting',
    //     'note' => 'Discounts on hardshells and packs for club members.',
    // ),

);
